import React from 'react';
import '../../styles/Loader.css'

const Loader = () => {
    return (
        <div className='loading-corps'>
            <span className="loader">Loading...</span>
        </div>
    );
};

export default Loader;